@model MainModel

@{
    ViewData["Title"] = "Home Page";
}

<!-- 스타일 추가 -->
<style>
    .labelBox {
        border: 1px solid #000;
        padding: 10px;
        margin-bottom: 10px;
        display: block; /* 변경된 부분 */
    }

    .dataBox {
        border: 1px solid #000;
        padding: 10px;
        margin-top: 10px;
        display: block; /* 변경된 부분 */
    }
</style>

@foreach (var item in Model.Total_historyData)
{
    <!-- LOT_ID 레이블 출력 -->
    <div class="labelBox">LOT_ID</div>

    <!-- SERIAL 레이블 출력 -->
    <div class="labelBox">SERIAL</div>
}